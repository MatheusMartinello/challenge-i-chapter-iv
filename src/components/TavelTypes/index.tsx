import { Flex, HStack, Spacer, Stack } from "@chakra-ui/react";
import LinkTravel from "./linktravel";

export default function TravelTypes() {
  return (
    <Flex align="center" justify="center" w={"100%"} mt={10}>
      <LinkTravel />
    </Flex>
  );
}
